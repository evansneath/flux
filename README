Flux v0.1 (pre-alpha)

The Flux project is a dynamic, real-time system for the emulation of various
sources of audio effects environments.

More details about the Flux project can be read on our wiki:
    https://github.com/evansneath/flux/wiki

To run the Flux main loop, simply execute the following code in src/core:
    python main.py